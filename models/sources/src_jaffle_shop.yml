version: 2

sources:
  - name: jaffle_shop
    database: postgres
    schema: public
    tables:
      - name: raw_customers
        columns:
          - name: id
            tests:
              - unique
              - not null
      - name: raw_orders
        columns:
          - name: id
            tests:
              - unique
              - not null
      - name: raw_payments
        columns:
        - name: id
          tests:
            - unique
            - not null